# Example .env file

# Head to https://openweathermap.org
# Create an account if you don't have one
# Click on the `API keys` sub-heading
# Use the default key or generate new key and name it
# Ensure key status reads "Active", copy and paste it below
API_KEY=your_actual_api_key_goes_here